# MoeMail Local v0.21.10

修复账户页切换“始终允许加载外部资源”时，整个账户面板卸载重建、其他设置草稿丢失的问题。

保存偏好后改为后台重新读取服务端会话并同步各标签页，不再切换全局登录加载状态。保留开启前的风险确认、服务端保存、跨标签同步和权限未确认时的安全兜底。

## 验证

- 生产构建、TypeScript、修改文件 ESLint 与权限兜底检查通过。
- 浏览器实际验证开启和关闭均正常；其他面板的未保存草稿保留，第二个标签页同步更新。

本补丁不新增数据库迁移或配置项。Docker 发布流水线沿用既有检查和四类镜像的 amd64/arm64 发布流程。
